#!/bin/bash


echo "type * ptr          = a pointer of type named ptr"
echo "*ptr                = the value of whatever pts is pointed at"
echo "*(ptr + i)          = the value of whatever ptr is ponited at plus i"
echo "&thing              = the address of thing"
echo "type *ptr = &thing  = a pointer of type named ptr set to the address of thing"
echo "ptr++               = increment where ptr points"
echo "ptr -> element      = use pointer to struct/union to get element"
